#pragma once

#include "flipperhack_structs.h"

// Default actions
extern void item_default_use_consumable(); // stubbed for now
//void item_default_inspect(void* state_void, void* item_void);

// Helper to use an item (handles null checks)
//void item_use(void* state, uint8_t item_id);

// Helper to inspect an item
//void item_inspect(void* state, Item* item);
